---
import Heads from "@/components/Heads.astro";
import Footer from "@/components/sections/Footer.astro";
import Navbar from "@/components/sections/Navbar.astro";

export interface Props {
  title: string;
  author: string;
  description: string;
  articleDate?: Date;
}

const { title, author, description, articleDate } = Astro.props as Props;
---

<!doctype html>
<html lang="en">
  <head>
    <Heads title={title} author={author} description={description} />
  </head>
  <body>
    <Navbar />
    <slot />
    <Footer />
    <style is:global>
      @font-face {
        font-family: "Hind";
        src: url("/fonts/Hind/hind-latin-400-normal.woff") format("woff");
        font-weight: 300;
        font-style: normal;
        font-display: swap;
      }

      @font-face {
        font-family: "Hind Bold";
        src: url("/fonts/Hind/hind-latin-500-normal.woff") format("woff");
        font-style: normal;
        font-display: swap;
      }

      @font-face {
        font-family: "Open Sans";
        src: url("/fonts/OpenSans/open-sans-cyrillic-800-normal.woff")
          format("woff");
        font-style: normal;
        font-weight: 700;
        font-display: swap;
      }

      :root {
        --black-c-900: color-mix(
          in lch,
          hsl(221, 76%, 5%),
          hsl(120, 24%, 96%) 5%
        );
        --black-c-800: color-mix(
          in lch,
          hsl(221, 76%, 5%),
          hsl(120, 24%, 96%) 15%
        );

        --white-c-900: color-mix(
          in lch,
          hsl(120, 24%, 96%),
          hsl(221, 76%, 5%) 5%
        );
        --white-c-800: color-mix(
          in lch,
          hsl(120, 24%, 96%),
          hsl(221, 76%, 5%) 15%
        );

        --primary-color: var(--black-c-900);
        --secondary-color: hsl(160, 73%, 36%);
        --secondary-lighter__color: hsl(273, 88%, 68%);
        --blue-color: hsl(212, 100%, 70%);
        --white-color: var(--white-c-900);

        --dark-rb-gradient: linear-gradient(
          90deg,
          var(--primary-color) 0%,
          var(--secondary-color) 100%
        );
        --hero__dark-bg: linear-gradient(-200deg, #232243 -79%, #010613 90%);
      }

      @media (prefers-color-scheme: light) {
        :root {
          color: var(--white-c-800);
          --hero__light-bg: linear-gradient(
            169deg,
            #f9f8f8 -169.26%,
            #dbe5fc 144.3%
          );
        }

        :root.dark {
          --hero__dark-bg: linear-gradient(-200deg, #232243 -79%, #010613 90%);
          --primary-color: var(--black-c-900);
        }
      }

      /* CSS Reset */
      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      * {
        padding: 0;
        margin: 0;
      }

      h1,
      h2,
      h3,
      h4,
      h5,
      h6 {
        text-wrap: balance;
      }

      p {
        text-wrap: pretty;
        line-height: 1.7rem;
        font-size: clamp(1.3rem, 1.24rem + 0.14vw, 1.5rem);
      }

      html {
        scroll-behavior: smooth;
      }

      a {
        text-decoration: none;
      }

      html,
      body {
        position: relative;
        font-size: 100%;
        color-scheme: dark light;

        ::-webkit-scrollbar {
          width: 0.87rem;
        }

        ::-webkit-scrollbar-thumb {
          border: 4px solid transparent;
          background-clip: padding-box;
          border-radius: 9999px;
          background-color: #9797f7;
        }

        ::-webkit-scrollbar-track {
          box-shadow: inset 0 0 3.2rem #0e0e1b;
          cursor: pointer !important;
        }
      }

      button {
        border: none;
        background-color: transparent;
      }

      ul {
        padding: 0;
      }

      li {
        list-style: none;
      }

      h1 {
        font-family: "Opens Sans", system-ui, sans-serif;
        font-size: clamp(1.5rem, 2.01rem + 1.39vw, 5rem);
      }

      h2 {
        font-family: "Hind", system-ui, sans-serif;
        font-size: clamp(1.5rem, 1.05rem + 1.53vw, 3rem);
        font-weight: 700;
      }

      h3 {
        font-family: "Hind", sans-serif;
        font-size: 3.5rem;
      }

      h4 {
        font-family: "Hind", sans-serif;
        font-size: 1.5rem;
      }

      img {
        display: block;
        max-width: 100%;
      }

      :is(p, a, li, small, button, label, input, textarea) {
        font-family: "Hind", sans-serif;
      }
    </style>
  </body>
</html>
